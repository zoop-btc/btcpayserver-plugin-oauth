@using BTCPayServer.Abstractions.Extensions
@using BTCPayServer.Plugins.OAuth.Views
@using Microsoft.AspNetCore.Mvc.TagHelpers

@if (User.IsInRole("ServerAdmin"))
{
<li class="nav-item">
    <a asp-controller="UIOAuth" asp-action="Index" class="nav-link @ViewData.IsActivePage(OAuthPluginNavPages.Index)">
        <span> &#xf505; OAuthPlugin</span>
    </a>
</li>
}